{
  "name": "@yeetful/agent-kit",
  "version": "0.1.0",
  "private": true,
  "description": "Shared payer plumbing for Yeetful's example agents. Each agent stays a thin adapter; this kit owns the expense account (the yeetful() grant-aware paid fetch) + receipt log + the LIVE gate, so spend-control wiring is one place, not N agents.",
  "type": "module",
  "exports": {
    ".": "./src/index.ts"
  },
  "scripts": {
    "typecheck": "tsc --noEmit",
    "lint": "echo \"(no lint)\"",
    "test": "echo \"(kit covered via agent tests)\"",
    "build": "echo \"(source package — consumed via workspace)\""
  },
  "dependencies": {
    "viem": "^2.21.0",
    "yeetful": "^0.10.1"
  },
  "devDependencies": {
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
