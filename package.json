{
  "name": "agent-examples",
  "version": "0.1.0",
  "private": true,
  "description": "Yeetful's fleet of x402 *payer* agents — the mirror image of x402-services. One repo, many thin agents, each with an expense account (allowlist + per-call/per-day USD caps + receipts). Shared payer plumbing lives in @yeetful/agent-kit.",
  "packageManager": "pnpm@8.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "test": "turbo run test",
    "typecheck": "turbo run typecheck"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "^5.7.3"
  }
}
